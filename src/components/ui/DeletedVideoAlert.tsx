export function DeletedVideoAlert() {
  return (
    <div
      role="alert"
      className="rounded-sm bg-black/60 px-3 py-2 text-sm font-semibold text-white"
      aria-label="사용할 수 없는 영상"
    >
      사용할 수 없는 영상
    </div>
  );
}
